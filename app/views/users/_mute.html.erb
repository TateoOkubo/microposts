<%= form_for(current_user.muting_relationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :muted_user_id, @user.id %></div>
  <%= f.submit "ミュート", class: "btn btn-primary" %>
<% end %>
